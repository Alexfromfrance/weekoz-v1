if (document.querySelector(".btn-submit-login-tareum")){
document.querySelector(".btn-submit-login-tareum").addEventListener('click', (event) => {
  const errorMessage = document.querySelector('.errors')
  errorMessage.innerHTML = "Incorrect Email/Password. \<br>Please review the errors below:"
})
}
